/* tslint:disable:no-unused-variable */

import { AddChartDlgComponent } from './add-chart.component';

describe('Component: Add Chart Dialog', () => {
    it('should create an instance', () => {
        const component = new AddChartDlgComponent();
        expect(component).toBeTruthy();
    });
});
