/* tslint:disable:no-unused-variable */

import { CreateProjectDlgComponent } from './create-project.component';

describe('Component: Create Project Dialog', () => {
    it('should create an instance', () => {
        const component = new CreateProjectDlgComponent();
        expect(component).toBeTruthy();
    });
});
